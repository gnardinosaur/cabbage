<h1>Create New Portfolio</h1>

<h3><%= link_to @logged_in_user.full_name, user_path(@logged_in_user) %></h3> <!-- add nav bar with link back to user or style this link using css -->

<h3>Available Cash to Invest: <%= number_to_currency(@logged_in_user.balance) %></h3>

<%= form_for @portfolio do |f| %> 

    <h4>Portfolio Name: <%= f.text_field(:title) %></h4>
    
    <%= f.submit "Create" %>
    
<% end %>



